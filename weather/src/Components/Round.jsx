const Round = (props) => {
  const { width, height, mtt, className } = props;
  return (
    <div
      className={`fixed top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2  rounded-full w-${width} h-${height} border border-gray-400 `}
    >
      asdasdasd
    </div>
  );
};
export default Round;
